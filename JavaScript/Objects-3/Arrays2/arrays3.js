/*
Javascript Array Constructor:
        Here, we need to create an instance of array by passing argumentsin constructor so that we dont have to provide value explicitly.
*/
var emp = new Array("Rajoli", "Sunil", "Kumar");
for(i = 0; i < emp.length; i++){
    console.log(emp[i]);
}