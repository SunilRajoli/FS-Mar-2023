/*
Javascript History Object:
            Javacsript History Object represents the array of URLs visited by the user.
            By using this object, u can load previous, forward or any particular page.
            Accessed by window.history
Property:
            length - return the length of history URLs.
Metods:
            forward() - loads the next page.
            back() - loads the previous page.
            go() - loads the given page.
*/
